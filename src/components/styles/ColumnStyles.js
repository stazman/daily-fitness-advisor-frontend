import styled from 'styled-components'

const ColumnStyles = styled.div`
  .col-lg-12.message-col {
    min-height: 10em;
    padding-top: 5em;
    text-align: center;
  }

  .col-lg-12.button-col {
    min-height: 8em;
    text-align: center;
  }
`

export default ColumnStyles