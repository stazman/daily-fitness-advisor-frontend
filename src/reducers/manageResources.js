const manageResources = (state = {resources: []}, action) => {
  switch (action.type){
    case 'FETCH_RESOURCES':
      return state
    
    default:
      return state
  }
}

export default manageResources